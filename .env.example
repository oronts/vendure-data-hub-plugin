# DataHub Plugin Development Server Environment Variables
# Copy this file to .env and adjust values as needed

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Vendure API server port (default: 3000)
PORT=3000

# Vendure base URL for asset server and external references
# Default: http://localhost:${PORT}
VENDURE_BASE_URL=http://localhost:3000

# Vite development server port for dashboard hot reload (default: 5173)
VITE_DEV_PORT=5173

# API host for vite dashboard plugin (default: http://localhost)
API_HOST=http://localhost

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Cookie secret for session management (REQUIRED for production)
COOKIE_SECRET=dev-secret-change-in-production

# Superadmin credentials (default: superadmin/superadmin)
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=superadmin

# =============================================================================
# DATABASE (for production - dev uses SQLite)
# =============================================================================

# DATABASE_URL=postgres://user:password@localhost:5432/vendure
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=vendure
# DATABASE_USERNAME=vendure
# DATABASE_PASSWORD=secret

# =============================================================================
# EXTERNAL SERVICES (for pipeline integrations)
# =============================================================================

# Demo API key for testing REST API extractors
# DEMO_API_KEY=your-api-key

# CRM API key for customer export pipelines
# CRM_API_KEY=your-crm-key

# Google Merchant Center credentials
# GOOGLE_MERCHANT_KEY=your-google-key

# Facebook Catalog credentials
# FACEBOOK_CATALOG_KEY=your-fb-key
