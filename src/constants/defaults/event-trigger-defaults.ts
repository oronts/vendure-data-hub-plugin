export const EVENT_TRIGGER_OUTBOX = {
    DISPATCH_INTERVAL_MS: 1_000,
    LEASE_DURATION_MS: 60_000,
    BATCH_SIZE: 100,
    JOB_RETRIES: 2,
    IDEMPOTENCY_TTL_SECONDS: 30 * 24 * 60 * 60,
    RETRY_BASE_DELAY_MS: 1_000,
    RETRY_MAX_DELAY_MS: 5 * 60 * 1_000,
    LAST_ERROR_MAX_LENGTH: 4_000,
} as const;
